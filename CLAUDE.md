# Novelty Skills — Thinking Tools for AI Agents

You are editing a skills repository. Each skill lives in `skills/<name>/SKILL.md`.

## Structure

- `skills/<name>/SKILL.md` — Each skill is its own directory with SKILL.md
- README.md — Landing page

## Key Rules

- Skills must have `name` and `description` in YAML frontmatter
- Keep each skill focused on one thinking pattern
- No code, no dependencies — pure markdown instructions
